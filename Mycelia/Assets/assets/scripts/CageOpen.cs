using UnityEngine;

public class CageOpen : MonoBehaviour
{
    private bool isOpen = false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (isOpen)
        {
            
        }
    }
}
